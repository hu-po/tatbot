defaults:
  - override /mcp: default

host: "192.168.1.51"
port: 8000
extras: [bot, dev, gen, img, viz, gpu]
tools:
  - reset
  - align
  - sense
  - stroke
  - ping_nodes
  - list_nodes
  - list_scenes
  - convert_strokelist_to_batch